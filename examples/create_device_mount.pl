#!/usr/bin/perl
use warnings;
use strict;
use cryptsetup_pl;

my $user_name = "david_kennedy";
my $mount_root_path = "_mount_root";
my $crypt_devices_dir = "_crypt_devices";
my $crypt_keyfiles_dir = "_crypt_keyfiles";
